node_modules/
dist/
.env
.env.local
# The live PGlite cluster is generated; only the seeded snapshot is committed.
data/*
!data/sitesignal-seed.tar.gz
*.local
.DS_Store
node_modules/.tmp/

# Replit workspace state (the .replit config itself IS committed)
.config/
.upm/
.cache/
replit.nix.bak
coverage/
